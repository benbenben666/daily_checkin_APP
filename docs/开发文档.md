# daily_checkin_APP 开发文档

> **文档版本**：v1.2（后端全部接口实现并实测通过；前端经典 uni-app (Vue3) 首版实现）
> **数据库**：MySQL 8.0.16+ / InnoDB / utf8mb4 —— **已创建，9 张表全部验证通过**
> **状态**：后端已完成（认证/公司/成员/审批/任务/管理员/附件签名），前端页面首版完成；待确认事项见第 10 章
>
> ⚠️ **项目名称说明**：仓库名为 `daily_checkin_APP`、Java 包名为 `com.ben.daily_check_in`，但本项目**不是签到应用**，而是公司内任务管理系统。名称是早期遗留，是否重命名见第 10 章 Q6。

---

## ⚠️ 维护约定（改代码前必读）

**本项目的每一次改动 —— 功能新增、字段变更、流程调整、权限规则修改、技术栈更换 —— 都必须同步更新本文档。**

具体改动对应要更新哪几节：

| 改了什么 | 必须同步的位置 |
|---|---|
| 加/删字段、加/删表 | 5.1 表清单 · 5.2 实体关系 · 5.3 表结构；**同时改 `backend/src/main/resources/db/schema.sql`** |
| 改业务规则 | 4. 功能需求（对应编号 T-x / M-x / C-x / A-x / F-x）· 7. 核心业务流程 |
| 改角色权限 | 3. 角色与权限矩阵 |
| 做了新的技术取舍 | 6. 关键设计决策（新增一节，不要改写已有章节编号） |
| 加/改接口 | 8. 接口设计草案 |
| 用掉了待确认项 | 从 10.2 移到 10.1，并写上结论 |
| 换技术栈 / 加依赖 | 9. 技术栈与工程结构 |

**两条硬规则**：

1. **`db/schema.sql` 是表结构的唯一权威版本** —— 文档中的表结构描述与它不一致时，以 `schema.sql` 为准
2. **表结构变更必须同时改两处** —— `schema.sql` 和本文档 5.3，缺一不可

---

## 目录

1. [项目概述](#1-项目概述)
2. [术语表](#2-术语表)
3. [角色与权限](#3-角色与权限)
4. [功能需求](#4-功能需求)
5. [数据库设计](#5-数据库设计)
6. [关键设计决策](#6-关键设计决策)
7. [核心业务流程](#7-核心业务流程)
8. [接口设计草案](#8-接口设计草案)
9. [技术栈与工程结构](#9-技术栈与工程结构)
10. [待确认事项](#10-待确认事项)

---

## 1. 项目概述

### 1.1 定位

一个**公司内部的任务发布与执行平台**。公司管理层可以发布任务，员工执行任务并提交成果，所有过程留痕可查。

### 1.2 核心功能

| 模块 | 功能 |
|---|---|
| 账号 | 手机号注册登录；系统管理员由后台手工指定 |
| 公司 | 创建公司（自动成为创始人）；凭邀请码申请加入；解散公司 |
| 成员 | 申请与审批；角色管理；移除成员；拉黑成员 |
| 任务 | 发布任务（全局/指定）；限时控制；抢单与指派；完成提交；超时补交；取消 |
| 附件 | 任务详情配图、完成提交图片（阿里云 OSS） |
| 记录 | 任务全量留痕，按角色控制可见范围 |

### 1.3 两层多租户结构

```
一人多公司  ——  同一个用户可以在多家公司里有身份
一公司多人  ——  一家公司有多名成员
身份独立    ——  张三可以是 A 公司的创始人，同时是 B 公司的员工，
                同时还是系统里的普通用户
```

---

## 2. 术语表

| 术语 | 含义 |
|---|---|
| 系统角色 | `user.system_role`：用户在整个系统中的身份（USER / ADMIN） |
| 公司角色 | `user_company.company_role`：用户在**某一家公司内**的身份（FOUNDER / MANAGER / EMPLOYEE） |
| 全局任务 | `task_type = GLOBAL`，全公司成员都可执行，先到先得（抢单式） |
| 指定任务 | `task_type = ASSIGNED`，只能由被指派人执行，可指派多人 |
| 参与 | 员工与某个任务的关联关系：全局任务 = 我抢到并完成；指定任务 = 我被指派 |
| 拉黑 | 公司层面永久拒绝某用户加入，记录在 `company_blacklist` |
| 超时完成 | 任务已过 `deadline_at` 之后才提交完成，`is_late = 1` |

---

## 3. 角色与权限

### 3.1 系统级角色

| 角色 | 说明 | 获取方式 |
|---|---|---|
| `USER` | 普通用户 | 注册即得（数据库默认值） |
| `ADMIN` | 系统管理员（开发者级） | **只能手工改库**，注册接口不提供 |

**`ADMIN` 权限范围**（"什么都能做，直接就是开发者"）：

- 添加 / 删除管理员
- 删除用户、修改任意用户信息
- 解散任意公司（不受"只有创始人能解散"限制）
- 拥有独立的前端页面

### 3.2 公司级角色

| 角色 | 发布任务 | 审批申请 | 拉黑 | 移除成员 | 取消任务 | 解散公司 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| `FOUNDER` 创始人 | ✅ | ✅ 全部 | ✅ | ✅ | ✅ | ✅ 仅本公司 |
| `MANAGER` 管理者 | ✅ | ✅ 仅员工申请 | ✅ | ✅ | ✅ | ❌ |
| `EMPLOYEE` 员工 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| `ADMIN` 系统管理员 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ 任意公司 |

### 3.3 谁能执行任务

**所有公司成员都可以执行任务**，包括创始人和管理者：

- 全局任务：全公司成员（含创始人和管理者）都能抢
- 指定任务：创始人和管理者之间**可以互相指派**

### 3.4 任务可见性

任务的可见性由**两个彼此独立的维度**共同决定，不要混淆：

| 维度 | 字段 | 决定什么 | 取值 |
|---|---|---|---|
| **可见范围** | `task.visibility` | **谁能看见**这个任务 | `PUBLIC` 全部人可见 / `RESTRICTED` 仅指定人可见 |
| **任务类型** | `task.task_type` | **谁能执行**这个任务 | `GLOBAL` 全公司可抢 / `ASSIGNED` 仅被指派人 |

四个组合都成立：

| 组合 | 效果 |
|---|---|
| `GLOBAL` + `PUBLIC` | 全公司可见、全公司可抢（最典型的"抢单任务"） |
| `GLOBAL` + `RESTRICTED` | 只有白名单能看见，看见的人都能抢 |
| `ASSIGNED` + `PUBLIC` | 全公司可见，但只有被指派人能点完成 |
| `ASSIGNED` + `RESTRICTED` | 只有白名单可见，且只有被指派人能执行 |

**除了任务级的可见范围，还有一层人级的数据范围**：

| 视角 | 数据范围 |
|---|---|
| 创始人 / 管理者 / 系统管理员 | 本公司**全部**任务（含所有历史记录） |
| 员工（待抢视图） | 通过可见性校验 **且**（待抢的全局任务 **或** 指派给自己的任务） |
| 员工（历史视图） | 通过可见性校验 **且**（自己完成过 **或** 指派给自己的任务） |

**可见性校验**：`visibility = 'PUBLIC'` **或** 我在 `task_viewer` 白名单里 **或** 我是被指派人。

> 被指派人**不需要**重复写进 `task_viewer` —— 查询时自动可见，避免同一份名单在两处维护。

---

## 4. 功能需求

### 4.1 账号

| 编号 | 需求 |
|---|---|
| A-1 | 用户以**手机号**注册，手机号全局唯一，一个手机号只能有一个账号 |
| A-2 | 密码加密存储，绝不存明文 |
| A-3 | 注册产生的新用户系统角色固定为 `USER` |
| A-4 | **管理员无法通过注册成为**，只能由人工修改数据库 |
| A-5 | 「删除用户」为软删除（`status = 0`），不做物理删除（见 6.5） |

### 4.2 公司

| 编号 | 需求 |
|---|---|
| C-1 | 任何用户都可以创建公司，创建者自动成为该公司**创始人** |
| C-2 | 公司 ID 由数据库自增保证唯一 |
| C-3 | 公司**名称允许重名** |
| C-4 | 每家公司有唯一的**邀请码**，是加入公司的唯一凭据（因为名称可重名，无法定位） |
| C-5 | 解散公司：**仅创始人可解散本公司**；管理者不可以；**系统管理员可解散任意公司** |

### 4.3 成员

| 编号 | 需求 |
|---|---|
| M-1 | 用户凭**邀请码**申请加入公司 |
| M-2 | 申请时选择身份：`MANAGER` 或 `EMPLOYEE`（**不可申请 `FOUNDER`**） |
| M-3 | 同一用户对**同一家公司**每天最多申请 **3 次** |
| M-4 | 有一条待审批申请时，不能再申请同一家公司 |
| M-5 | 撤回、被拒、通过、待审批**全部计入**每日 3 次限额 |
| M-6 | 被拒绝后可以再次申请（受 M-3 限额约束） |
| M-7 | **申请历史完整保留**，可追溯被拒几次 |
| M-8 | 审批权限：申请 `MANAGER` 只能由**创始人**审批；申请 `EMPLOYEE` 由创始人和管理者共同审批；**公司当前没有管理者时一律回退给创始人** |
| M-9 | 创始人/管理者可**拉黑**成员；拉黑后该用户**永久**不能申请该公司 |
| M-10 | 被拉黑者若**在职**，同时被踢出公司；若**非在职**，仅禁止申请 |
| M-11 | 若被拉黑者有**待审批**申请，该申请立即变为「已拒绝」（申请人看到的是被拒绝，不暴露拉黑事实） |
| M-12 | 创始人/管理者可**移除**在职成员（不拉黑） |
| M-13 | 用户可**主动退出**公司 |
| M-14 | 退出/被移除后可重新申请；审批通过时复用原成员记录行 |
| M-15 | 「主动退出 / 被移除 / 被拉黑」三种离职方式必须可区分 |

### 4.4 任务

| 编号 | 需求 |
|---|---|
| T-1 | 创始人、管理者可发布任务 |
| T-2 | 任务包含**标题**和**详情**；详情支持**上传图片** |
| T-3 | 任务可设置**限时时长**，范围 **5 分钟 ~ 30 天**（5 ~ 43200 分钟） |
| T-4 | 任务类型一：**全局任务**（`GLOBAL`）—— 全公司成员（含创始人和管理者）都可执行，**先到先得，一人完成即结束** |
| T-5 | 任务类型二：**指定任务**（`ASSIGNED`）—— 指派给指定成员，**可指派多人**；创始人和管理者之间可互相指派 |
| T-6 | 任务状态：`PENDING` 未完成 / `EXPIRED` 已超时 / `COMPLETED` 已完成 / `CANCELLED` 已取消 |
| T-7 | 完成任务时可提交**文字描述**、**图片**，也**允许什么都不填**直接提交 |
| T-8 | **超时后是否允许补交**由发布时设置的 `allow_late_submit` 开关控制，**默认允许**；补交窗口长度 **等于限时时长本身**；超时提交的内容标记为「超时完成」 |
| T-9 | 取消任务：**本公司创始人和管理者**都可以执行 |
| T-10 | 所有任务**全部留痕**，不做物理删除 |
| T-11 | 任务的**可见范围**可设为「全部人可见」或「指定人可见」；选后者时必须指定可见人白名单 |
| T-12 | 任务发布后**可修改**：发布者和具有管理权限的人（创始人 / 管理者）都可以改，且**所有字段都可改** —— 包括任务类型（`GLOBAL` ↔ `ASSIGNED` 可以互改）和可见范围 |
| T-13 | 任务可见性按 3.4 的规则控制 |

### 4.5 附件

| 编号 | 需求 |
|---|---|
| F-1 | 图片存储使用**阿里云 OSS** |
| F-2 | 数据库只存 OSS `object_key`，不存完整 URL |
| F-3 | 图片分两类：任务详情配图（`DETAIL`）、完成提交图（`SUBMIT`） |

---

## 5. 数据库设计

### 5.1 表清单

| # | 表名 | 职责 |
|---|---|---|
| 1 | `user` | 用户账号 |
| 2 | `company` | 公司 |
| 3 | `user_company` | 成员关系（在职 / 已离职） |
| 4 | `join_application` | 加入申请与审批（完整历史） |
| 5 | `company_blacklist` | 公司黑名单 |
| 6 | `task` | 任务（含完成信息） |
| 7 | `task_assignee` | 任务指派关系 |
| 8 | `task_image` | 任务图片 |
| 9 | `task_viewer` | 任务可见人白名单 |

> **可执行建表脚本见** `backend/src/main/resources/db/schema.sql`（该文件是唯一权威版本，本文档如有出入以它为准）。

### 5.2 实体关系

```
                         ┌──────────────────┐
                         │       user       │
                         │  phone / role    │
                         └────────┬─────────┘
        ┌──────────────┬──────────┴──────────┬───────────────┐
        │              │                     │               │
        ▼              ▼                     ▼               ▼
┌───────────────┐ ┌──────────────┐ ┌────────────────┐ ┌─────────────┐
│ user_company  │ │join_applicat │ │company_blacklist│ │    task     │
│ 成员关系      │ │  -ion 申请   │ │   黑名单        │ │  任务       │
└───────┬───────┘ └──────┬───────┘ └───────┬────────┘ └──────┬──────┘
        │                │                 │                 │
        │                │                 │        ┌────────┴────────┐
        │                │                 │        ▼                 ▼
        │                │                 │  ┌──────────────┐ ┌────────────┐
        │                │                 │  │task_assignee │ │ task_image │
        │                │                 │  │  任务指派    │ │  任务图片  │
        │                │                 │  └──────────────┘ └────────────┘
        └────────────────┴─────────────────┴─────────────────┘
                                  │
                                  ▼
                          ┌───────────────┐
                          │   company     │
                          │ 邀请码/解散   │
                          └───────────────┘
```

**注意**：`join_application`、`company_blacklist`、`task` 各有**多个外键指向 `user`**，所以约束名必须各不相同。

### 5.3 各表结构

#### 5.3.1 `user` 用户表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | 用户 ID |
| `phone` | VARCHAR(20) | NOT NULL, **UK** | 手机号，登录账号 |
| `password` | VARCHAR(255) | NOT NULL | 密码哈希 |
| `nickname` | VARCHAR(50) | NULL | 昵称 |
| `avatar_key` | VARCHAR(500) | NULL | 头像 OSS key |
| `system_role` | VARCHAR(20) | NOT NULL, DEFAULT `'USER'`, CHECK | USER / ADMIN |
| `status` | TINYINT | NOT NULL, DEFAULT 1, CHECK | 1-正常 0-禁用 |
| `created_at` / `updated_at` | DATETIME | 数据库自动维护 | |

#### 5.3.2 `company` 公司表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | 公司 ID |
| `name` | VARCHAR(100) | NOT NULL, IDX | 名称，**允许重名** |
| `invite_code` | CHAR(8) | NOT NULL, **UK** | 邀请码 |
| `status` | TINYINT | NOT NULL, DEFAULT 1, CHECK | 1-正常 0-已解散 |
| `dissolved_by` | BIGINT | NULL, FK→user | 解散操作人 |
| `dissolved_at` | DATETIME | NULL | 解散时间 |

> 本表**不含 `founder_id`** —— 创始人由 `user_company` 中 `company_role = 'FOUNDER'` 唯一表达。

#### 5.3.3 `user_company` 成员关系表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `user_id` | BIGINT | NOT NULL, FK→user | |
| `company_id` | BIGINT | NOT NULL, FK→company | |
| `company_role` | VARCHAR(20) | NOT NULL, CHECK | FOUNDER / MANAGER / EMPLOYEE |
| `status` | TINYINT | NOT NULL, DEFAULT 1, CHECK | 1-在职 0-已离职 |
| `leave_type` | TINYINT | NULL, CHECK | 1-主动退出 2-被移除 3-被拉黑 |
| `joined_at` | DATETIME | NOT NULL | 加入时间 |
| `left_at` | DATETIME | NULL | 离职时间 |
| `founder_only` | TINYINT | **生成列** STORED | 内部使用，约束每公司唯一创始人 |

**关键约束**：

- `uk_user_company (user_id, company_id)` —— 一人一公司只能有一条成员记录
- `uk_one_founder (company_id, founder_only)` —— 每公司最多一个创始人
- `ck_uc_leave_consistency` —— `status` 与 `leave_type` / `left_at` 联动，防止矛盾数据

#### 5.3.4 `join_application` 加入申请表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `user_id` | BIGINT | NOT NULL, FK→user | 申请人 |
| `company_id` | BIGINT | NOT NULL, FK→company | 目标公司 |
| `apply_role` | VARCHAR(20) | NOT NULL, CHECK | MANAGER / EMPLOYEE（不含 FOUNDER） |
| `status` | VARCHAR(20) | NOT NULL, DEFAULT `'PENDING'`, CHECK | PENDING / APPROVED / REJECTED / CANCELLED |
| `reviewer_id` | BIGINT | NULL, FK→user | 审批人 |
| `reviewed_at` | DATETIME | NULL | 审批时间 |
| `reject_reason` | VARCHAR(200) | NULL | 拒绝理由 |
| `created_at` | DATETIME | NOT NULL | 申请时间（限额计数依据） |
| `pending_only` | TINYINT | **生成列** STORED | 内部使用，限制只能有一条待审批 |

#### 5.3.5 `company_blacklist` 公司黑名单表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `company_id` | BIGINT | NOT NULL, FK→company | |
| `user_id` | BIGINT | NOT NULL, FK→user | 被拉黑者 |
| `operator_id` | BIGINT | NOT NULL, FK→user | 操作人 |
| `reason` | VARCHAR(200) | NULL | 拉黑原因 |
| `created_at` | DATETIME | NOT NULL | 拉黑时间 |

**有记录 = 永久拉黑**，`uk_company_user (company_id, user_id)` 保证唯一。

#### 5.3.6 `task` 任务表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `company_id` | BIGINT | NOT NULL, FK→company | 所属公司 |
| `publisher_id` | BIGINT | NOT NULL, FK→user | 发布人 |
| `task_type` | VARCHAR(20) | NOT NULL, CHECK | GLOBAL / ASSIGNED |
| `title` | VARCHAR(200) | NOT NULL | 标题 |
| `description` | TEXT | NULL | 详情文字 |
| `time_limit_minutes` | INT | NOT NULL, CHECK 5~43200 | 限时时长 —— **唯一需要人工填写的时间字段** |
| `deadline_at` | DATETIME | **生成列** STORED | 完成截止 = `created_at` + 限时时长 |
| `late_deadline_at` | DATETIME | **生成列** STORED | 补交截止 = `deadline_at` + 限时时长 |
| `status` | VARCHAR(20) | NOT NULL, DEFAULT `'PENDING'`, CHECK | PENDING / EXPIRED / COMPLETED / CANCELLED |
| `completed_by` | BIGINT | NULL, FK→user | 完成人 |
| `completed_at` | DATETIME | NULL | 完成时间 |
| `submit_content` | TEXT | NULL | 提交文字（可空） |
| `cancelled_by` | BIGINT | NULL, FK→user | 取消人 |
| `cancelled_at` | DATETIME | NULL | 取消时间 |
| `cancel_reason` | VARCHAR(200) | NULL | 取消原因 |
| `visibility` | VARCHAR(20) | NOT NULL, DEFAULT `'PUBLIC'`, CHECK | PUBLIC / RESTRICTED |
| `allow_late_submit` | TINYINT | NOT NULL, DEFAULT 1, CHECK | 1-允许超时补交 0-不允许 |
| `is_late` | TINYINT | **生成列** STORED | 0-按时 1-超时完成 |

**关键约束**：`ck_task_completion` —— 状态为 `COMPLETED` 时必须有完成人和完成时间，其他状态必须都没有。

#### 5.3.7 `task_assignee` 任务指派表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `task_id` | BIGINT | NOT NULL, FK→task | |
| `user_id` | BIGINT | NOT NULL, FK→user | 被指派人 |
| `assigned_at` | DATETIME | NOT NULL | 指派时间 |

`uk_task_user (task_id, user_id)` —— 同一任务不能重复指派同一人。

#### 5.3.8 `task_image` 任务图片表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `task_id` | BIGINT | NOT NULL, FK→task | |
| `image_type` | VARCHAR(20) | NOT NULL, CHECK | DETAIL / SUBMIT |
| `object_key` | VARCHAR(500) | NOT NULL | OSS 对象键 |
| `file_name` | VARCHAR(255) | NULL | 原始文件名 |
| `file_size` | INT | NULL | 字节 |
| `sort_order` | INT | NOT NULL, DEFAULT 0 | 展示顺序 |
| `created_at` | DATETIME | NOT NULL | 上传时间 |

#### 5.3.9 `task_viewer` 任务可见人白名单

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK, AI | |
| `task_id` | BIGINT | NOT NULL, FK→task | |
| `user_id` | BIGINT | NOT NULL, FK→user | 可见用户 |
| `created_at` | DATETIME | NOT NULL | 添加时间 |

**只在 `task.visibility = 'RESTRICTED'` 时生效。** `uk_task_user (task_id, user_id)` 保证同一任务不重复添加同一人。

> 被指派人**不需要**写进本表 —— 查询时"是否被指派"会独立判断，自动可见。

---

## 6. 关键设计决策

### 6.1 用中间表而非在 `user` 上加 `company_id`

一个人属于多家公司，挂在 `user` 上无法表达。中间表还承载了**关系自身的属性**（角色、加入时间、离职时间），这是多对多建模的标准做法。

### 6.2 「申请」为什么必须独立成表

申请和成员关系是**两种生命周期完全不同的实体**：

| | 成员关系 | 申请 |
|---|---|---|
| 性质 | 长期状态 | 一次性事件 |
| 生命周期 | 加入 → 在职 → 离职 | 提交 → 审批 → 终结 |
| 独有属性 | 加入时间、离职时间 | 申请角色、审批人、审批时间、拒绝理由 |

若塞进 `user_company`：所有在职员工的审批字段永远是 NULL（语义脏）；「被拒绝」与「已退出」共用同一状态值无法区分；`uk_user_company` 只允许一行，会丢失「被拒 3 次」的历史。

### 6.3 「拉黑」为什么必须独立成表

- 一个**从没加入过**公司的人也能被拉黑 → `user_company` 里没有行可改
- 一个**在职**的人也能被拉黑 → 与 `uk_user_company` 唯一键冲突
- 拉黑记录本身携带审计信息（谁拉的、什么时候、为什么）

### 6.4 生成列 + 唯一索引：MySQL 缺失的「部分唯一约束」

MySQL 不支持 PostgreSQL 的 `UNIQUE ... WHERE 条件`，但**唯一索引允许多个 NULL**。在唯一索引前垫一个生成列，即可实现"只对满足条件的行做唯一约束"：

| 业务需求 | 生成列表达式 | 唯一索引 |
|---|---|---|
| 每公司最多一个创始人 | `IF(company_role='FOUNDER', 1, NULL)` | `(company_id, founder_only)` |
| 每人每公司最多一条待审批 | `IF(status='PENDING', 1, NULL)` | `(user_id, company_id, pending_only)` |

非目标行算出 `NULL`，NULL 在唯一索引中互不冲突 —— 于是"非目标行可以有任意多条，目标行只能有一条"。

### 6.5 「删除用户」只能是软删除

`user.id` 被以下位置引用：`user_company`、`join_application`（申请人 + 审批人）、`company_blacklist`（被拉黑者 + 操作人）、`task`（发布人 + 完成人 + 取消人）、`company.dissolved_by`。外键默认 `RESTRICT`，物理删除必然失败。

更重要的是：**硬删除会破坏任务历史**（"谁完成的"变成空白），与"所有任务都要留痕"直接冲突。

**因此"删除用户" = `UPDATE user SET status = 0`（禁用）**，登录时校验 `status = 1`。

### 6.6 约束分层：哪些交给数据库，哪些交给业务层

| 约束 | 由谁保证 | 理由 |
|---|---|---|
| 手机号唯一 | **数据库** | 并发下只有唯一索引靠得住 |
| 不能重复加入同一公司 | **数据库** | 同上 |
| 每公司最多一个创始人 | **数据库** | 业务层容易漏 |
| 拉黑永久生效 | **数据库** | "永久"语义放数据库最稳 |
| 同公司只能有一条待审批 | **数据库** | 防止刷爆审批队列 |
| 枚举值合法 | **数据库**（CHECK） | 挡住 `'admin'`、`'ADMN'` 等脏值 |
| 状态与附属字段一致 | **数据库**（CHECK） | 如"已完成却没有完成人" |
| 引用完整性 | **数据库**（外键） | 防止孤儿数据 |
| **每天最多申请 3 次** | **业务层** | 见下 |
| 谁能审批哪种申请 | **业务层** | 权利矩阵 |
| 拉黑时是否踢人 | **业务层** | 动作编排 |
| 完成任务时的抢单原子性 | **数据库**（条件 UPDATE + 行锁） | 见 6.7 |

**为什么「一天 3 次」不放数据库？** 可以写成触发器，但触发器同样防不住并发（两个请求同时 COUNT 到 2 然后都插入）。既然数据库层做不到完全正确，就不值得为它增加触发器的维护成本。业务层 COUNT 一次即可，偶发超到 4 次无实质危害。

**判断标准**：这是**数据正确性**问题，还是**频率**问题？前者交给数据库，后者业务层足够。

### 6.7 ⭐ 抢单的原子性（本项目最重要的一点）

全局任务"只要有人做就可以"，意味着**两个人可能同时点完成**。

**绝对不能这么写**：

```java
Task t = taskMapper.selectById(id);
if ("PENDING".equals(t.getStatus())) {          // ← 两个人同时读到 PENDING
    taskMapper.updateStatus(id, "COMPLETED");   // ← 两个人都更新成功
}
```

**正确做法是一条带条件的 UPDATE，靠 InnoDB 行锁保证原子性**（完整 SQL 见 7.4）。判断结果只看影响行数：`1` = 抢到，`0` = 被别人抢先 / 已取消 / 无权限。

这一个 `UPDATE` 同时完成「检查权限 + 检查状态 + 写入完成」，中间没有缝隙给并发钻，**不需要分布式锁，也不需要 `synchronized`**。

### 6.8 「已超时」与「超时完成」

`status` 的四个值性质并不相同：

| 状态 | 性质 | 谁来改 |
|---|---|---|
| `PENDING` | 事实 | 发布时写入 |
| `COMPLETED` | **人的决策** | 执行人提交 |
| `CANCELLED` | **人的决策** | 管理者 |
| `EXPIRED` | **时间的函数** | 没人点它，它自己会到 |

由于用户允许**超时补交**，状态机变成：

```
                    ┌──按时完成──> COMPLETED (is_late = 0)
                    │
PENDING ────────────┼──超时──> EXPIRED ──补交──> COMPLETED (is_late = 1)
                    │
                    └──取消──> CANCELLED
```

**「是否超时完成」用生成列推导，而不是新增第 5 个状态值**：

```sql
`is_late` TINYINT GENERATED ALWAYS AS (
    IF(`completed_at` IS NOT NULL AND `completed_at` > `deadline_at`, 1, 0)
) STORED
```

理由：它**完全由 `completed_at` 和 `deadline_at` 决定**，是推导值而非独立事实。用生成列意味着：

1. **不可能标错** —— 不存在"代码里判断分支写反"的情况
2. **不需要额外写入逻辑** —— 提交时只管写 `completed_at`
3. **与 `founder_only`、`pending_only` 一脉相承** —— 全设计统一遵循"推导出来的事实不手工维护"

**边界**：正好卡在截止时刻提交算**按时**（用 `>` 而非 `>=`）。

### 6.9 图片表为什么只有一张（带 `image_type`）

任务有两个地方能传图：发布时的详情配图、完成时的提交图。它们**生命周期与所属任务完全一致**，所以一张表 + 一个类型列即可。

**与 6.2 / 6.3 的区别**：那两张表独立是因为**生命周期不同**；图片只是**用途不同**。判断标准是生命周期，不是"看起来像不像"。

### 6.10 图片存 OSS `object_key` 而非完整 URL

完整 URL 里的域名会变（换 CDN、绑自定义域名、切内外网 endpoint、加防盗链签名）。存完整 URL 一旦域名变更就要**批量 UPDATE 全表**；存 key 则只需改一行拼接代码。

### 6.11 「可见范围」与「任务类型」是两个正交维度

最初的设计里只有 `task_type`，它被迫同时承担两个职责。加上可见范围后必须拆开：

| 维度 | 决定什么 | 字段 |
|---|---|---|
| **执行权限** | 谁能**点完成** | `task_type` |
| **数据可见性** | 谁能**看见** | `visibility` |

**为什么不能合并成一个字段？** 因为四种组合都有真实业务含义（见 3.4）："指定任务但全公司可见"是常见的公示场景；"全局任务但只有部分人可见"则是有针对性的抢单。

**实现上的一个关键细节**：被指派人**不必**写进 `task_viewer`。可见性查询把"是否被指派"作为独立条件判断，而不是在写入时把指派人同步进白名单 —— 后者会让同一份名单存在两处，需要时刻保持一致，正是本项目一直在避免的问题（同 6.2 / 6.3 的思路）。

### 6.12 三个时间列全部由数据库算，应用层不碰

你的原话是：*"任务截止时间是从开始时自动开始算的，选五分钟就在发布那一刻自动算截止"*。

这句话说明**截止时间不是独立字段，而是 `发布时间 + 限时时长` 的推导结果**。既然如此，就不该交给应用层去算：

```
time_limit_minutes   ← 用户唯一需要填的时间字段
deadline_at          = created_at + time_limit_minutes        （生成列）
late_deadline_at     = deadline_at + time_limit_minutes       （生成列）
is_late              = completed_at > deadline_at             （生成列）
```

**好处**：

1. 应用层**根本没有写入截止时间的机会**，也就不可能写错
2. 编辑任务时只需改 `time_limit_minutes`，两个截止时间自动跟着变
3. 与 6.4 的 `founder_only`、`pending_only` 一致 —— 推导出来的事实不手工维护

**代价**：不能脱离时长单独调整截止时间。将来若需要"延长截止但保持时长不变"，要把 `deadline_at` 改回普通列。

**补交窗口语义**：你说的"默认补交的时间就是等同于任务完成时间限制"，落地为 **补交窗口长度 = 限时时长本身**。

**实测验证**（MySQL 8.0.44 临时表，用最终定义跑的真实结果）：

| 发布时刻 | 限时 | 完成截止 | 补交截止 | 完成时刻 | `is_late` |
|---|---|---|---|---|---|
| 14:00 | 30 分钟 | 14:30 | 15:00 | 14:20 | **0** 按时 |
| 14:00 | 30 分钟 | 14:30 | 15:00 | **14:30 整** | **0** 卡点算按时 |
| 14:00 | 30 分钟 | 14:30 | 15:00 | 14:45 | **1** 超时完成 |
| 14:00 | 30 分钟 | 14:30 | 15:00 | 未完成 | 0 |
| 14:00 | 5 分钟 | 14:05 | 14:10 | — | — |
| 14:00 | 43200 分钟（30 天） | 10-23 14:00 | 11-22 14:00 | — | — |

---

## 7. 核心业务流程

### 7.1 注册

```sql
-- 注意：不写 system_role，由数据库默认值给出 'USER'
INSERT INTO `user` (`phone`, `password`, `nickname`) VALUES (?, ?, ?);
```

### 7.2 创建公司

```
1. INSERT company (name, invite_code, ...)        -- 生成唯一邀请码
2. INSERT user_company (user_id, company_id, 'FOUNDER', status=1)
   ↑ 与上一步在同一事务中
```

`uk_one_founder` 会在数据库层保证每家公司不会出现第二个创始人。

### 7.3 申请加入公司

```
1. 查 company_blacklist  → 有记录 → 拒绝
2. 查 user_company (status=1) → 已是成员 → 拒绝
3. 查今日申请次数（同公司）≥ 3 → 拒绝
4. 查是否已有 PENDING → 有 → 拒绝
5. INSERT join_application (status='PENDING', apply_role=?)
```

今日次数查询（**注意不要在列上加函数**）：

```sql
SELECT COUNT(*) FROM `join_application`
WHERE `user_id` = ? AND `company_id` = ?
  AND `created_at` >= CURDATE()
  AND `created_at` <  CURDATE() + INTERVAL 1 DAY;
-- 不加 status 条件 → 撤回、拒绝、通过、待审批全部计入 3 次
```

### 7.4 完成任务 / 超时补交

```sql
UPDATE `task` t
LEFT JOIN `task_assignee` ta ON ta.`task_id` = t.`id` AND ta.`user_id` = :me
SET t.`status`         = 'COMPLETED',
    t.`completed_by`   = :me,
    t.`completed_at`   = NOW(),
    t.`submit_content` = :content
WHERE t.`id` = :taskId
  AND t.`status` IN ('PENDING', 'EXPIRED')   -- 未完成或已超时
  AND (t.`status` = 'PENDING'                        -- 未超时：随时可提交
       OR (t.`allow_late_submit` = 1                 -- 已超时：开关必须打开
           AND NOW() <= t.`late_deadline_at`))       -- 且仍在补交窗口内
  AND (t.`task_type` = 'GLOBAL'              -- 全局任务谁都能做
       OR ta.`id` IS NOT NULL);               -- 指定任务只有被指派人能做
```

`is_late` 不需要手工写入，由数据库自动推导。

`LEFT JOIN task_assignee` 是为了让"指定任务只有被指派人能做"这个校验也在同一条语句内完成，否则先查一次指派表就又产生了竞态窗口。

### 7.5 取消任务

```sql
UPDATE `task`
SET `status` = 'CANCELLED', `cancelled_by` = ?, `cancelled_at` = NOW(), `cancel_reason` = ?
WHERE `id` = ? AND `status` IN ('PENDING', 'EXPIRED');   -- 已完成/已取消的不能再取消
```

### 7.6 定时标记超时（Spring `@Scheduled`，每分钟）

```sql
UPDATE `task` SET `status` = 'EXPIRED'
WHERE `status` = 'PENDING' AND `deadline_at` < NOW();
```

> ⚠️ 定时任务存在最长一分钟的延迟。因此在 7.4 的完成逻辑里**必须同时校验状态**（`status IN ('PENDING','EXPIRED')`），不能只依赖定时任务的及时性。

### 7.7 审批

```sql
-- 通过（事务）
START TRANSACTION;
UPDATE `join_application`
SET `status` = 'APPROVED', `reviewer_id` = ?, `reviewed_at` = NOW()
WHERE `id` = ? AND `status` = 'PENDING';     -- 影响 0 行说明已被他人处理，回滚

INSERT INTO `user_company` (`user_id`, `company_id`, `company_role`, `status`, `joined_at`, `left_at`)
VALUES (?, ?, ?, 1, NOW(), NULL) AS new
ON DUPLICATE KEY UPDATE                       -- 曾离职则复用原行，避免撞唯一键
    `company_role` = new.`company_role`,
    `status` = 1, `leave_type` = NULL, `joined_at` = new.`joined_at`, `left_at` = NULL;
COMMIT;

-- 拒绝
UPDATE `join_application`
SET `status` = 'REJECTED', `reviewer_id` = ?, `reviewed_at` = NOW(), `reject_reason` = ?
WHERE `id` = ? AND `status` = 'PENDING';

-- 申请人撤回
UPDATE `join_application`
SET `status` = 'CANCELLED'
WHERE `id` = ? AND `user_id` = ? AND `status` = 'PENDING';
```

审批权限判断：

```sql
-- 公司当前有没有管理者（走 idx_company_status_role）
SELECT EXISTS (
    SELECT 1 FROM `user_company`
    WHERE `company_id` = ? AND `status` = 1 AND `company_role` = 'MANAGER'
) AS has_manager;
```

| 申请身份 | 谁能批 |
|---|---|
| `MANAGER` | 仅创始人 |
| `EMPLOYEE` | 创始人 或 管理者 |
| 公司无管理者 | 一律回退创始人 |

### 7.8 拉黑（在职 / 非在职合并为一条代码路径）

```sql
START TRANSACTION;

-- ① 写黑名单（幂等，重复拉黑不报错）
INSERT INTO `company_blacklist` (`company_id`, `user_id`, `operator_id`, `reason`)
VALUES (?, ?, ?, ?) AS new
ON DUPLICATE KEY UPDATE
    `operator_id` = new.`operator_id`,
    `reason`      = new.`reason`;        -- created_at 保留最初拉黑时间

-- ② 踢出公司 —— 只在职时生效，否则影响 0 行
UPDATE `user_company`
SET `status` = 0, `leave_type` = 3, `left_at` = NOW()
WHERE `user_id` = ? AND `company_id` = ? AND `status` = 1;

-- ③ 待审批申请 → 显示为被拒绝（不暴露拉黑事实）
UPDATE `join_application`
SET `status` = 'REJECTED', `reject_reason` = '申请未通过', `reviewed_at` = NOW()
WHERE `user_id` = ? AND `company_id` = ? AND `status` = 'PENDING';

COMMIT;
```

**①+③ 恒执行，② 靠 `WHERE status = 1` 自动变成空操作** —— 所以"在职"和"非在职"两种场景走同一个方法，无需分支。

### 7.9 移除成员（不拉黑）

```sql
UPDATE `user_company`
SET `status` = 0, `leave_type` = 2, `left_at` = NOW()
WHERE `user_id` = ? AND `company_id` = ? AND `status` = 1;
```

### 7.10 解散公司

```sql
-- 创始人只能解散自己的；系统管理员可解散任意
UPDATE `company`
SET `status` = 0, `dissolved_by` = ?, `dissolved_at` = NOW()
WHERE `id` = ? AND `status` = 1;
```

### 7.11 可见性查询

```sql
-- 创始人 / 管理者 / 系统管理员：本公司全部任务
SELECT * FROM `task` WHERE `company_id` = ?;

-- 员工：待抢视图（可见 且（待抢的全局任务 或 指派给自己））
SELECT * FROM `task` t
WHERE t.`company_id` = ?
  -- ① 可见性校验
  AND (
        t.`visibility` = 'PUBLIC'
     OR EXISTS (SELECT 1 FROM `task_viewer`  tv WHERE tv.`task_id` = t.`id` AND tv.`user_id` = :me)
     OR EXISTS (SELECT 1 FROM `task_assignee` ta WHERE ta.`task_id` = t.`id` AND ta.`user_id` = :me)
  )
  -- ② 数据范围
  AND (
        (t.`task_type` = 'GLOBAL' AND t.`status` = 'PENDING' AND t.`deadline_at` > NOW())
     OR EXISTS (SELECT 1 FROM `task_assignee` ta2 WHERE ta2.`task_id` = t.`id` AND ta2.`user_id` = :me)
  );

-- 员工：历史视图（可见 且 我参与过的）
SELECT * FROM `task` t
WHERE t.`company_id` = ?
  AND (
        t.`visibility` = 'PUBLIC'
     OR EXISTS (SELECT 1 FROM `task_viewer`  tv WHERE tv.`task_id` = t.`id` AND tv.`user_id` = :me)
     OR EXISTS (SELECT 1 FROM `task_assignee` ta WHERE ta.`task_id` = t.`id` AND ta.`user_id` = :me)
  )
  AND (
        t.`completed_by` = :me
     OR EXISTS (SELECT 1 FROM `task_assignee` ta2 WHERE ta2.`task_id` = t.`id` AND ta2.`user_id` = :me)
  );

-- 单条任务的可见性校验（进详情页前）
SELECT EXISTS (
    SELECT 1 FROM `task` t
    WHERE t.`id` = ?
      AND (t.`visibility` = 'PUBLIC'
        OR EXISTS (SELECT 1 FROM `task_viewer`   tv WHERE tv.`task_id` = t.`id` AND tv.`user_id` = :me)
        OR EXISTS (SELECT 1 FROM `task_assignee` ta WHERE ta.`task_id` = t.`id` AND ta.`user_id` = :me))
) AS can_view;

-- 统计超时完成
SELECT COUNT(*) FROM `task`
WHERE `company_id` = ? AND `status` = 'COMPLETED' AND `is_late` = 1;
```

---

## 8. 接口设计草案

> 统一前缀 `/api`。除注册登录外均需鉴权（`Authorization: Bearer <token>`）。
> 统一响应包装：`{ code, message, data }`，`code = 0` 成功，非 0 为 HTTP 语义错误码（400/401/403/404/409/500）。
> **v1.2 起本章与后端实现一致。**

### 8.1 认证

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 手机号注册（不写 system_role，数据库默认 USER） |
| POST | `/api/auth/login` | 登录，返回 JWT token 与用户信息 |
| GET | `/api/auth/me` | 当前用户信息（含系统角色，前端据此切页面） |
| PUT | `/api/auth/me` | 修改自己的昵称 |

**认证实现**：自研 JWT（jjwt 0.12.6）+ Spring 拦截器。令牌只携带用户 ID，拦截器每次请求从数据库现查用户状态与角色（禁用、改角色立即生效）。密码用 BCrypt（仅引入 `spring-security-crypto`，不引入完整 Spring Security）。未选 Sa-Token 的原因：Spring Boot 4.1.1 过新，第三方 starter 兼容性无保证。

### 8.2 公司

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/companies` | 创建公司（自动成为创始人，同事务写 user_company） |
| GET | `/api/companies/mine` | 我所在的公司及角色 |
| GET | `/api/companies/{id}` | 公司详情（邀请码仅创始人/管理者/系统管理员可见） |
| POST | `/api/companies/join` | 凭邀请码申请加入 |
| GET | `/api/companies/{id}/members` | 成员列表 |
| DELETE | `/api/companies/{id}/members/{userId}` | 移除成员 |
| POST | `/api/companies/{id}/blacklist` | 拉黑（在职踢出 + 待审批置拒绝，同一事务） |
| POST | `/api/companies/{id}/dissolve` | 解散公司 |

> 解除拉黑接口（DELETE blacklist）暂未实现，需要时再加。

### 8.3 申请与审批

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/companies/{id}/applications` | 待审批列表 |
| GET | `/api/companies/{id}/applications/history` | 全部申请历史 |
| GET | `/api/applications/mine` | 我提交的申请 |
| POST | `/api/applications/{id}/approve` | 通过（事务：条件更新申请 + upsert 成员行） |
| POST | `/api/applications/{id}/reject` | 拒绝 |
| POST | `/api/applications/{id}/cancel` | 申请人撤回 |

### 8.4 任务

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/companies/{id}/tasks` | 发布任务 |
| GET | `/api/companies/{id}/tasks` | 任务列表（`?view=history` 员工历史视图；管理者看全部） |
| GET | `/api/tasks/{id}` | 任务详情（含图片/指派人/可见人） |
| PUT | `/api/tasks/{id}` | 编辑任务（发布者或创始人/管理者，所有字段可改） |
| POST | `/api/tasks/{id}/complete` | 完成 / 超时补交（条件 UPDATE 保证抢单原子性，见 6.7） |
| POST | `/api/tasks/{id}/cancel` | 取消任务 |

### 8.5 附件

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/files/policy?bizType=detail\|submit` | 获取 OSS PostObject 直传凭证（objectKey + 上传地址 + 签名字段） |

> 签名用纯 JDK（HmacSHA1 + Base64）实现，未引入 OSS SDK。`bizType`：detail-详情配图，submit-完成提交图。

### 8.6 系统管理（仅 ADMIN）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/users` | 用户列表（`?keyword=` 模糊搜索手机号/昵称） |
| PUT | `/api/admin/users/{id}` | 修改用户信息（昵称/状态） |
| DELETE | `/api/admin/users/{id}` | 删除用户（= 软删除，status 置 0） |
| PUT | `/api/admin/users/{id}/role` | 设为 / 取消管理员 |
| GET | `/api/admin/companies` | 全部公司 |
| POST | `/api/admin/companies/{id}/dissolve` | 解散任意公司 |

---

## 9. 技术栈与工程结构

### 9.1 后端（已实现）

| 组件 | 版本 | 来源 |
|---|---|---|
| Spring Boot | **4.1.1** | `backend/pom.xml` |
| Java | **21** | 同上 |
| MyBatis | `mybatis-spring-boot-starter` **4.1.0**（XML 映射，`resources/mapper/*.xml`） | 同上 |
| MySQL 驱动 | `mysql-connector-j` | 同上 |
| JWT | `jjwt-api/impl/jackson` **0.12.6** | 同上 |
| 密码加密 | `spring-security-crypto`（BCrypt，仅加密模块） | 同上 |
| Lombok | 已配置注解处理器 | 同上 |
| 构建 | Maven Wrapper（Maven 3.9.16） | `backend/.mvn` |

> 注：Spring Boot 4.x 的 starter 名称有变化，`pom.xml` 中使用的是 `spring-boot-starter-webmvc`（而非 3.x 的 `spring-boot-starter-web`）。

**后端分层**：`controller`（HTTP 入口）→ `service`（业务 + 权限校验 + 事务）→ `mapper`（MyBatis 接口 + XML）。公共层：`common.Result` 统一响应、`common.BizException` + `GlobalExceptionHandler` 统一异常。安全层：`security.JwtUtil` / `AuthInterceptor` / `UserContext`（ThreadLocal 存当前用户）。定时任务：`task.TaskExpireScheduler`（每分钟标记超时，见 7.6）。

### 9.2 前端（经典 uni-app + Vue3）

> ⚠️ **v1.2 重大变更**：前端已从 uni-app x（UTS + .uvue）**更换为经典 uni-app (Vue3)**（.vue + JS，Vite 构建）。原 uni-app x 脚手架已删除。原因：用户明确要求"vue3 的 uniapp"，且经典版生态成熟、可跑浏览器便于调试。

| 项目 | 实际值 | 依据 |
|---|---|---|
| 框架 | **经典 uni-app（Vue3，CLI 创建）** | `package.json` 中 `@dcloudio/*` 3.0.0-4080720251210001 |
| 语言 | JavaScript + `.vue` 单文件组件 | `src/**/*.vue` |
| 构建 | **Vite 5.2.8** + `@dcloudio/vite-plugin-uni` | `vite.config.js` |
| 运行 | `npm run dev:h5`（浏览器 H5，默认 5173 端口） | `package.json` scripts |
| 网络请求 | `uni.request`（封装于 `src/api/request.js`） | |
| 路由 | `src/pages.json` 配置式 | |
| 令牌存储 | `uni.setStorageSync('auth_token')` | |

**页面清单**（`src/pages/`）：login 登录注册、index 我的公司、company/create 创建公司、company/join 加入公司、company/home 公司主页（任务/成员/审批三 Tab，按角色分流）、task/create 发布任务、task/detail 任务详情（完成/补交/取消）、admin/index 系统管理、profile/index 个人中心。

**接口基地址**：`src/api/request.js` 中 `BASE_URL = 'http://localhost:8080'`（对应 Q15 的落地方式：全局常量 + 后端已开 CORS）。

### 9.3 第三方服务

| 服务 | 用途 | 状态 |
|---|---|---|
| 阿里云 OSS | 图片存储（任务详情图、完成提交图） | 后端直传签名接口已实现；AccessKey/Bucket 为**占位配置**，接入前替换 `application.yaml` 的 `app.oss.*` |

### 9.4 工程结构（按仓库实际状态）

```
daily_checkin_APP/
├── backend/                                     # Spring Boot 后端
│   ├── pom.xml
│   ├── mvnw / mvnw.cmd
│   └── src/main/
│       ├── java/com/ben/daily_check_in/
│       │   ├── DailyCheckInApplication.java     # @EnableScheduling + @EnableConfigurationProperties
│       │   ├── common/                          # Result / BizException / GlobalExceptionHandler
│       │   ├── config/                          # AppProperties / WebMvcConfig(拦截器+CORS)
│       │   ├── security/                        # JwtUtil / AuthInterceptor / UserContext / LoginUser
│       │   ├── controller/                      # Auth/Company/Application/Task/File/Admin
│       │   ├── service/                         # 各模块业务逻辑
│       │   ├── mapper/                          # MyBatis 接口
│       │   ├── entity/                          # 9 张表实体
│       │   ├── dto/                             # 请求/响应对象（auth/company/application/task/admin）
│       │   └── task/                            # TaskExpireScheduler 定时标记超时
│       └── resources/
│           ├── application.yaml                 # 数据源 + JWT + OSS 占位配置
│           ├── mapper/*.xml                     # MyBatis SQL 映射
│           └── db/schema.sql                    # ← 建表脚本（表结构唯一权威版本）
├── frontend/
│   └── frontend/                                # ← 经典 uni-app 工程根（仍保留一层嵌套，见 10.2 Q13）
│       ├── package.json / vite.config.js / index.html
│       └── src/
│           ├── manifest.json / pages.json / main.js / App.vue / uni.scss
│           ├── api/                             # request.js 封装 + index.js 接口定义
│           └── pages/                           # 9 个页面
├── docs/
│   └── 开发文档.md                               # ← 本文档
├── .gitignore
└── .gitattributes
```

**关于 `frontend/frontend/` 多一层嵌套**：结构保留（功能无影响）。见 10.2 Q13。

### 9.5 数据库连接配置

`backend/src/main/resources/application.yaml` 已配置：

```yaml
spring:
  application:
    name: daily_check_in
  datasource:
    url: jdbc:mysql://localhost:3306/daily_check_in?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ben.daily_check_in.entity,com.ben.daily_check_in.dto
  configuration:
    map-underscore-to-camel-case: true   # user_id -> userId 的关键

app:
  jwt:
    secret: "..."        # ⚠️ 生产环境必须更换
    expire-hours: 168    # 7 天
  oss:                   # ⚠️ 占位配置
    endpoint: "oss-cn-hangzhou.aliyuncs.com"
    access-key-id: "your-access-key-id"
    access-key-secret: "your-access-key-secret"
    bucket: "your-bucket-name"
```

> ⚠️ 本仓库是 **public** 仓库，生产环境不要把明文密码提交上去，改用环境变量或忽略的 `application-local.yaml`。

---

## 10. 待确认事项

### 10.1 已确认

| # | 问题 | 结论 |
|---|---|---|
| Q1 | 取消任务的权限 | **本公司创始人和管理者**都可以取消 |
| Q2 | 超时补交 | 由发布时的 `allow_late_submit` 开关控制，**默认允许** |
| Q3 | 手机号注册要不要短信验证码 | **不需要**，手机号只当账号用 |
| Q5 | 前端技术栈 | **已定为经典 uni-app (Vue3)**（.vue + JS + Vite），原 uni-app x 脚手架已删除，见 9.2 |
| Q7 | 任务能否编辑 | **能**：发布者和有管理权限的人都能改，且**所有字段都可改**（含任务类型与可见范围）；并由此新增了**任务可见范围**需求，已加入 `task.visibility` + `task_viewer`。编辑接口 `PUT /api/tasks/{id}` 已实现 |
| Q10 | 补交时间的默认值 | 补交窗口长度 **= 限时时长本身**（限时 30 分钟的任务，超时后仍有 30 分钟可补交）。已实现为生成列 `late_deadline_at` |
| Q11 | 编辑时间限制的方式 | 截止时间**始终**由「发布时间 + 限时时长」自动推导，所以编辑时改的是 `time_limit_minutes`；`deadline_at` 与 `late_deadline_at` 已改为**生成列**，数据库层面杜绝手工写入 |
| Q12 | 前端 UI 组件库 | 已随 Q5 解决：经典 uni-app 下暂用原生组件 + 自写样式，后续可引入 `uni-ui` |
| Q15 | 前端接口基地址 | 已落地：`src/api/request.js` 全局常量 `BASE_URL = 'http://localhost:8080'`，后端 `WebMvcConfig` 已开 CORS |
| Q16 | 认证方案 | **自研 JWT（jjwt）+ 拦截器 + BCrypt**。未选 Sa-Token：Spring Boot 4.1.1 过新，第三方 starter 兼容性无保证；纯 JWT 的"无法作废"问题由"拦截器每次请求现查数据库用户状态"弥补（禁用立即失效） |

### 10.2 仍待确认

| # | 问题 | 影响 |
|---|---|---|
| **Q4** | **公司解散后**，成员关系和未完成任务如何处理？保持原状只隐藏，还是把成员全部置为离职、未完成任务全部取消？当前实现：仅把 `company.status` 置 0，成员与任务保持原状，前端公司列表自动过滤已解散公司 | 仅业务层；若要在 `company` 上记录解散时的处理结果则需加字段 |
| **Q6** | **项目/数据库/包名是否重命名**？仓库名 `daily_checkin_APP`、库名 `daily_check_in`、包名 `com.ben.daily_check_in` 都是"签到"语义，与实际的任务管理不符 | 重命名涉及 git 仓库名，改动较大；保持现状也可 |
| **Q8** | **任务操作日志**（`task_log`）：你答复"以后再开发"，保留在待办中。当前只能靠 `task.updated_at` 知道"被改过"，但记录不了"改了什么" | 将来加表，不影响现有结构 |
| **Q9** | **被移除 / 离职的成员**，其历史任务记录还看得到吗？当前实现：列表/详情接口要求"在职成员"，离职后看不到 | 仅影响可见性查询 |
| **Q13** | **`frontend/frontend/` 的多一层嵌套要不要去掉**？（工程根在最内层） | 仅影响目录结构 |
| **Q14** | **IDEA 以「仓库根目录」为项目打开，要不要改成 `backend/`？** 经核实（2026-09-25）：以根目录打开**功能上完全正常** —— 根 `.idea/misc.xml` 已正确记录 `$PROJECT_DIR$/backend/pom.xml`，Maven 模块、JDK 21、本地仓库 `D:\repo`、`D:\conf\settings.xml` 全部识别正确。改成打开 `backend/` 的唯一好处是 IDEA 不再索引 uni-app 前端文件、编译输出落到 `backend/target/`；代价是看不到前端代码 | 仅影响本地 IDE 体验，**不影响项目正确性** |
| **Q17** | **图片上传前端直传尚未接入**：后端 `/api/files/policy` 已能签发凭证，但前端页面还没做选图/上传/回显（OSS 配置也是占位）。接入顺序：填真实 OSS 配置 → 前端选图 → 拿凭证直传 → 把 objectKey 提交给发布/完成接口 | 影响附件功能闭环 |

---

## 附录：设计原则速查

本项目在数据库设计中反复使用以下原则，新增功能时可参照：

1. **推导出来的事实不手工维护** —— 用生成列（`founder_only` / `pending_only` / `is_late`）
2. **生命周期不同 → 独立成表**；只是用途不同 → 加类型列（对比 6.2/6.3 与 6.9）
3. **正确性问题交给数据库，频率问题交给业务层**（6.6）
4. **并发靠条件 UPDATE + 行锁，不靠先查后写**（6.7）
5. **MySQL 没有部分唯一索引 → 生成列 + 唯一索引**（6.4）
6. **单表多个外键指向同一张表时，约束名必须不同**
